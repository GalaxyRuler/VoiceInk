# VoiceInk Windows Project Completion

Last updated: 2026-05-27

This tracker is an approximate parity bar for the full free/open-source Windows fork, grounded in the current parity spec and implemented Windows slices. Percentages represent user-feature parity against the macOS app, not just buildability.

## Overall

```text
VoiceInk Windows parity  [####################] 99%
```

## Area Bars

| Area | Completion | Bar | Status |
| --- | ---: | --- | --- |
| Core dictation pipeline | 99% | `[####################]` | Local recording/transcription/insertion, conservative default-on voice activity detection for silent recordings with macOS-aligned 250 ms minimum speech duration, Transcribe Audio picker/drag-drop/copy/save/per-file-enhance/restored-queue flow with macOS-supported audio extension intake and Windows decoder fallback, presenter-backed accessible names for audio queue rows, cleanup with persisted custom filler-word lists, Unicode normalization before dictionary replacement, macOS-default language/text-formatting settings, macOS-aligned formatting-before-dictionary-replacement order, spoken new-line/new-paragraph formatting commands, macOS-aligned paste-only trailing-space behavior, macOS-aligned enhancement-before-user-cleanup ordering for recorder and audio-file transcripts, enhancement fallback, prompt-trigger detection, completed/canceled/failed history writes, metrics, and canceling in-flight post-recording work are in place. Remaining work is deeper transcript polish and edge-case tuning. |
| Shell and tray | 99% | `[####################]` | Navigation shell, macOS-order Permissions route, Settings/About footer navigation placement aligned with WinUI guidance, notification-area tray icon, close-to-tray, Windows-native in-app notification InfoBar for actionable status/error/warning/success messages, visible notification-area/taskbar-corner-overflow menu guidance plus status/pin guidance in the tray hover tooltip, actionable Taskbar settings tray row for notification-area icon visibility, disabled tray status row for scan-friendly current state, single-instance Windows App SDK activation that redirects duplicate launches to restore the existing process/window, open-source About/diagnostics, dedicated History window routing, rich tray quick-setting submenus for transcription model/provider, enhancement prompt/provider/AI model, language, audio, context, and Power Mode, direct tray Paste Last Transcription, Paste Last Enhanced, and Retry Last Transcription actions, a local Start hidden to tray setting as the Windows adaptation of macOS menu-bar-only mode, and best-effort `TaskbarCreated` tray icon recovery after Explorer/taskbar recreation are in place. Remaining work is visual polish and rare native shell lifecycle edge cases. |
| Floating recorder | 99% | `[####################]` | Mini and top-center Notch styles show recording/processing state, elapsed time, presenter-driven footer hints for Stop/Cancel/transcribing/inserting, presenter-backed recorder chrome UI Automation names and HelpText, presenter-backed dynamic Prompt/Power Mode button names and HelpText, macOS-aligned 15-bar recorder waveform backed by testable Core presentation, non-activating Stop/Cancel controls, hover-dismissable no-activate Prompt/Power chooser panels, presenter-backed Power Mode button labels with default Auto indicator, and a gated live transcript preview panel with interim-result disclosure now backed by Deepgram, AssemblyAI, Mistral, ElevenLabs, Soniox, Speechmatics, and Cartesia interim results. Final visual polish remains. |
| Shortcuts | 99% | `[####################]` | Primary/secondary recording shortcuts now support key-based and modifier-only Toggle, Push to Talk, and Hybrid key-up modes; default-off middle-click recording with activation delay parity, paste last, paste enhanced, retry, cancel, open history, quick add, toggle enhancement, cycle Power Mode, direct per-rule Power Mode selection, floating-recorder Ctrl/Alt digit prompt/Power Mode slots, read-only recorder fields with explicit Record buttons, Windows-key rejection, documented F12 reserved-key rejection, duplicate assignment detection, actionable native `RegisterHotKey` conflict diagnostics when another app owns a shortcut, and WTS session-boundary cleanup for stuck pressed shortcut state after lock/unlock/desktop-ready transitions. Remaining work is only rare native registration edge-case polish that requires device-level/manual lifecycle evidence. |
| Model management | 99% | `[####################]` | Local Whisper catalog cards, direct GGML downloads, imported `.bin` references, top-level folder import for existing `.bin` model libraries, app-local model storage, safe model removal for downloaded app-local GGML files and external imported references, Windows Explorer reveal actions for downloaded and imported model files, default model selection, local library overview, action rows for download/import/default/repair/warmup, storage/import/backup/warmup guidance rows including expected `ggml-*.bin` filename, custom/fine-tuned model conversion guidance, model integrity/checksum guidance for imported or manually downloaded binaries, presenter-backed accessible names for model library action rows, storage guidance rows, and catalog cards, open-source whisper.cpp/Hugging Face download-source guidance, and GGML compatibility-check guidance, model path health checks with actionable repair and user-selected validation guidance, optional GGML magic-header validation for large `.bin` files, selected-model health rows with `ggml-*.bin` filename guidance, a direct repair/warmup action button, stale imported model cleanup, model-aware language selection, cloud provider metadata cards, and nonblocking warmup/preload exist. Remaining work is narrow visual/lifecycle polish. |
| Cloud transcription | 99% | `[####################]` | OpenAI-compatible adapter with endpoint-query `response_format` support, secure key storage, Custom/Groq/Deepgram/AssemblyAI/Mistral/ElevenLabs/Soniox/Speechmatics/Gemini/xAI/Cartesia presets with provider metadata cards, metadata-only provider test requests for every named preset, direct Deepgram batch requests with advanced endpoint query option preservation, AssemblyAI upload/transcript polling, Soniox async upload/transcription polling plus realtime preview, Speechmatics Jobs API batch transcription plus realtime preview, Gemini inline-audio and Files API generateContent transcription, xAI Grok STT batch transcription, Cartesia Ink Whisper batch transcription, Mistral Voxtral batch transcription through the multipart adapter plus realtime preview, ElevenLabs Scribe batch transcription plus realtime preview, Deepgram/AssemblyAI/Mistral/ElevenLabs/Soniox/Speechmatics/Cartesia live preview streaming, and a user-owned-key smoke runbook exist. Remaining cloud work is advanced provider-specific options. |
| AI enhancement | 99% | `[####################]` | OpenAI-compatible enhancement, Anthropic Messages API enhancement, native Ollama chat enhancement, Local CLI hooks, macOS-aligned System Default/Assistant/Chat/Email/Rewrite prompts, macOS-aligned context spelling-priority instructions, assistant context wrapping, custom prompts with icon/description metadata, trigger-word activation with macOS-style trigger hints in the Enhancement and Power Mode prompt pickers, visible Assistant Mode selected/available behavior guidance, Windows-native custom prompt reordering, context, retries/timeouts, secure keys, macOS-aligned OpenAI GPT-5 temperature handling, macOS-aligned provider/model reasoning parameter shaping for OpenAI/Gemini/Cerebras/Groq, explicit no-automatic-provider-fallback privacy guidance, bounded timeout/retry readiness guidance, visible short-phrase guard and timeout/retry behavior rows, toggle-enhancement shortcut, and dynamic OpenRouter/Ollama model refresh exist. Remaining work is visual polish and smaller assistant niceties. |
| Context features | 99% | `[####################]` | Clipboard context, selected-text context with clipboard fallback and visible action guidance, active-window process/title context, sanitized browser URL context with visible query/fragment stripping guidance, default-off local screen OCR context rendered with the macOS `<CURRENT_WINDOW_CONTEXT>` prompt tag and `Active Window`/`Application`/`Window Content` body format including macOS-style `No text detected via OCR`, macOS-aligned selected-text/clipboard-first prompt render order, region-aware OCR capture plumbing, visible numeric OCR region controls, a visual OCR region picker, display-targeted OCR region selection, Power Mode per-rule OCR context overrides, Enhancement context readiness summary rows, macOS-aligned prompt guidance that prioritizes spellings from vocabulary, clipboard, current-window OCR, selected text, and active app/site context when transcript terms sound similar, presenter-backed accessible names for context readiness/privacy/action rows, provider-aware context privacy boundary rows, explicit OCR capture-scope privacy rows, one-time screenshot lifetime/image-not-uploaded disclosure, accurate desktop screenshot OCR boundary guidance, installed OCR language-pack guidance, unavailable-source graceful-skip disclosure, and context action guidance rows exist with graceful degradation. Deeper context visual polish remains. |
| Power Mode | 99% | `[####################]` | Rule model, master Power Mode enable/disable toggle, macOS-style Persist Configured Preferences session restoration, process/title/browser URL matching with semicolon/newline multi-target alternatives, active-window target refresh/replace/append actions for multi-app and multi-site rules, best-effort installed app picker for Start Menu shortcut process targets, default fallback, page-level match and override precedence guidance, visible per-recording session-scope guidance, explicit recorder chooser selection, cycle shortcut, direct per-rule shortcuts, settings overlays including per-rule screen/OCR context override, history metadata, editor UI, inline validation feedback, post-insertion auto-send keys, macOS-style page/empty-state copy, and rich accessible rule rows for targets/overrides/shortcuts/status exist. Remaining work is only deeper visual layout polish. |
| Dictionary | 99% | `[####################]` | Vocabulary, replacements, sorting, quick add, import/export, pipeline integration, macOS-style section descriptions, counts, summary rows, rule application guidance rows including processing-order, Quick Add entry-point, JSON-not-CSV Unicode-safe format guidance, import-conflict, local backup workflow, comma-separated alias, replacement example guidance, macOS-aligned formatting-before-replacement guidance, and provider privacy-boundary guidance, empty states, local overview guidance, disabled replacement row presentation, richer vocabulary/replacement row details with status badges, and presenter-backed accessible names for repeated Dictionary rows bound into the WinUI templates exist. Remaining work is deeper flow/layout polish. |
| History | 99% | `[####################]` | Dedicated History window, SQLite detail metadata, stable paginated loading with explicit Load More for normal and searched history, search, retry, re-enhance from saved original text, copy actions for original/final/enhanced/AI request, paste, audio playback/open with waveform and rate controls, selected-item local analysis rows with provider timing, macOS-aligned AI enhancement terminology, audio storage state, retry/re-enhance provenance, and export-scope privacy disclosure, presenter-backed accessible names for dedicated and embedded History list/analysis rows, single and batch delete, single and selected CSV export, and privacy cleanup operations exist. Remaining work is deeper visual polish. |
| Metrics | 99% | `[####################]` | SQLite metrics, macOS-style hero/card dashboard presentation including a first-class Time Saved card, filters, local action and diagnostics summary rows including audio-duration diagnostics and Windows Diagnostic Data Viewer history/storage separation guidance, metric data/formula guidance rows including the 35 WPM and 5-keystroke typing baseline, local-only model performance interpretation guidance, explicit accuracy-boundary guidance that Metrics reports usage/performance rather than WER or transcript correctness, presenter-backed dashboard/data/action/diagnostics/model-performance row accessibility names, templated model performance rows with primary values, UI Automation names, and macOS-style faster/slower-than-realtime transcription badges, CSV export, and confirmed reset controls exist. Deeper visual diagnostics polish remains. |
| Settings | 99% | `[####################]` | Shortcut/cleanup/provider/audio controls, macOS-style custom filler-word add/remove chip editor, recorder style selection, default-on VAD toggle, default-off middle-click recording controls with activation delay, Permissions readiness routing, local settings backup import/export, privacy cleanup, reset onboarding, clipboard restore delay, paste method choices, launch at login, start hidden to tray with current-state summary, diagnostic log export with safe OS architecture and logical-processor context, recording feedback controls, built-in Windows start/stop sound choices, custom start/stop sound import/reset/test, recording feedback current-state summary for sounds/system mute/media pause, macOS-style section descriptions, local data-safety overview guidance, grouped-section find guidance, action summary rows, current-state rows for paste/clipboard/feedback/cleanup/tray startup, backup/import guidance rows with manual Windows-profile export disclosure, open-source update guidance rows with a public releases check action, diagnostics privacy/export guidance rows including bounded recent-event summary scope and optional Windows diagnostic-data separation, and presenter-backed accessible names for repeated Settings and diagnostics rows exist. Remaining work is only deeper form layout polish. |
| Audio input | 99% | `[####################]` | Device refresh, System Default/Custom/Prioritized modes, priority ordering with unavailable-device fallback, visible System Default and fallback health rows with Windows Sound settings and desktop-app microphone privacy toggle guidance, custom persistence, endpoint-ID backed identity, saved-name rebinding, unavailable fallback, startup capture rebinding, live Core Audio device-change refresh, persistent status notices, Device Health rows with Active/Available/Unavailable/Default badges, readable endpoint IDs for physical microphones, presenter-backed accessible names for health/privacy rows and priority rows, and a Permissions-page microphone privacy link exist. Remaining work is visual polish and potential future WASAPI capture polish. |
| Onboarding | 99% | `[####################]` | First-run setup covers model path, recommended local model download, microphone health, testable Windows microphone privacy action targets, exact "Let desktop apps access your microphone" guidance for source-built desktop-app access, manual Windows microphone settings fallback guidance including recent desktop-app microphone activity, Windows-native text insertion readiness guidance as the macOS Accessibility Access adaptation, optional local context-awareness/screen-OCR consent guidance, microphone settings, compact readiness summary rows with Windows permission and insertion guidance, action rows, progress-aware readiness checklist, guided setup stages, macOS-style hero promise/taglines, macOS-style try-it-out tutorial steps with final insertion/History verification, presenter-backed display/accessibility text for setup action, summary, stage, and tutorial rows, explicit UI Automation names for programmatic setup controls/list regions, presenter-backed accessible names for post-onboarding Permissions checklist rows, in-dialog audio input refresh, audio input choice, shortcut, basic usage, Settings reset, and a post-onboarding Permissions page. Richer visual flow polish remains. |
| Packaging | 99% | `[####################]` | Source-run docs, runtime workaround, repeatable self-contained dev ZIP packaging with bundled GPL `LICENSE.txt`, dev ZIP smoke validation including license-text checks, per-user Dev ZIP install/uninstall helpers with Start Menu shortcut creation, signed MSIX manifest/script foundation with bundled GPL `LICENSE.txt`, certificate-free packaging preflight, timestamped MSIX signing support with maintainer-overridable `TimestampServerUrl` and `TimestampDigestAlgorithm`, non-installing MSIX artifact validation including license text, identity, `TargetDeviceFamily`, capability, executable, and safety checks, optional signed-build artifact validation, a gated signed install/uninstall smoke helper with read-only signature/trust diagnostics, read-only Local Machine Trusted People signer status reporting, and an execute-time Authenticode `Valid` signature gate before `Add-AppxPackage`, installed app identity verification, launch identity reference, and post-uninstall cleanup verification, App Installer `.appinstaller` manifest generation/validation with optional on-launch update settings plus `.msix`/`.msixbundle` MainPackage URI guards, non-mutating WinGet `InstallerType: msix` manifest generation/validation for signed release metadata with GPL license fields, artifact-root-contained `SHA256SUMS.txt` release checksum generation, SPDX 2.2 SBOM readiness guidance, Windows App Certification Kit readiness guidance with default-off workflow evidence upload and nonzero exit enforcement, installer-smoke evidence validation for summary/WACK XML artifacts, project-owned Homelab metadata for dry-run release route planning with tested container/headless routing, and a manual self-hosted Windows installer-smoke workflow with safe evidence artifact upload exist. The release readiness report includes signing/trust checklist plus exact Local Machine Trusted People store guidance, `0x800B0109` trust troubleshooting, WinApp CLI local signing, App Installer identity references, WinGet manifest readiness, release checksum readiness, SBOM readiness, WACK command references, ActivityID diagnostics, App Installer operational-log guidance, timestamp URL/digest guidance, and launch identity guidance. Actual signed MSIX install/WACK smoke remains gated on maintainer-owned signing/trust setup and disposable Windows runner execution. |

## Current Slice

```text
Installer Smoke Evidence Validator  [####################] 100%
```

Completed:

- Wrote the Windows Installer Smoke Evidence Validator spec and implementation plan.
- Grounded the slice in Microsoft's Windows App Certification Kit report-output guidance and the existing manual installer-smoke workflow artifact.
- Added a packaging asset test that fails when the repo lacks a non-mutating evidence validator.
- Added `test-installer-smoke-evidence.ps1` to validate runner summary fields, optional install-smoke evidence, and optional WACK XML parseability.

## Near-Term Priority

1. Continue packaging toward actual signed MSIX install/uninstall/WACK smoke and release signing flow when a trusted maintainer signing setup is available.
2. Continue visual parity polish for Power Mode, Settings, Metrics, Dictionary, Enhancement, and onboarding pages.
3. Continue model lifecycle polish for local model discovery, validation, and repair flows.
4. Consider a future WASAPI capture backend if endpoint-native recording becomes necessary.
